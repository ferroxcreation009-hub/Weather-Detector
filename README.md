# Weather-Detector
Weather Detector — a frontend web app built with vanilla HTML, CSS &amp; JavaScript. Fetches live weather data via OpenWeatherMap API and displays temperature, humidity, wind speed with dynamic weather icons.
